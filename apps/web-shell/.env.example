# 浏览器开发态直连本地 Core（可选）。仅当设置 VITE_CORE_BASE 时，
# coreClient 在无 Electron 桥的纯浏览器 dev 模式直连真实 Core，否则回落演示 mock。
# VITE_CORE_TOKEN 必须与 Core 启动的 --session-token 一致。
# 复制为 .env.local 并填入实际值为本地开发所用（.env.local 已被 gitignore）。
VITE_CORE_BASE=http://127.0.0.1:18765
VITE_CORE_TOKEN=change-me